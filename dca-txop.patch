diff -r 2273154dbd59 -r 297f975148ba ns-3.15/src/wifi/model/dca-txop.cc
--- a/ns-3.15/src/wifi/model/dca-txop.cc	Thu Oct 18 22:09:15 2012 -0400
+++ b/ns-3.15/src/wifi/model/dca-txop.cc	Sun Oct 21 20:30:44 2012 -0400
@@ -239,6 +239,8 @@
   m_stationManager->PrepareForQueue (hdr.GetAddr1 (), &hdr,
                                      packet, fullPacketSize);
   m_queue->Enqueue (packet, hdr);
+  if(hdr.GetAddr1() == Mac48Address::GetBroadcast())
+	  m_dcf->StartBackoffNow(m_rng->GetNext(0, m_dcf->GetCw()));
   StartAccessIfNeeded ();
 }
